$kind: http-request
name: Reset Password (Trigger)
method: POST
url: '{{base_url_auth}}/auth/forgot-password'
order: 1300
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
body:
  type: json
  content: |-
    {
      "email": "alumno@buap.mx"
    }
