$kind: http-request
name: Send Bienvenida (via Import)
method: POST
url: '{{base_url_docentes}}/alumnos/importar/{{materia_id}}'
order: 1000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
body:
  type: formdata
  content:
    - key: archivo
      type: file
      value: []
