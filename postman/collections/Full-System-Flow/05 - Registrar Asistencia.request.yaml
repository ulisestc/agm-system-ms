$kind: http-request
name: 05 - Registrar Asistencia
method: POST
url: '{{base_url_asistencias}}/asistencias/registrar'
order: 500
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "alumno_id": "{{alumno_id}}",
      "materia_id": "{{materia_id}}",
      "token_qr": "{{token_qr}}"
    }
